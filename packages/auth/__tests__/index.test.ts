// packages/auth/__tests__/index.test.ts
import { helloAuth } from '../src/index';

describe('Auth package placeholder', () => {
  it('helloAuth should return the expected string', () => {
    expect(helloAuth()).toBe('hello-auth');
  });
});
